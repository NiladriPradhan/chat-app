/**
 * Why the file exists: Contains validation schemas and logic for friendships.
 * Its responsibility: Ensures incoming request data meets required formats and constraints before processing.
 * When it will be used: Applied as middleware in friendships routes before reaching the controller.
 */
