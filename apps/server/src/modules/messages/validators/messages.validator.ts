/**
 * Why the file exists: Contains validation schemas and logic for messages.
 * Its responsibility: Ensures incoming request data meets required formats and constraints before processing.
 * When it will be used: Applied as middleware in messages routes before reaching the controller.
 */
