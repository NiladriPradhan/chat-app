/**
 * Why the file exists: Contains validation schemas and logic for auth.
 * Its responsibility: Ensures incoming request data meets required formats and constraints before processing.
 * When it will be used: Applied as middleware in auth routes before reaching the controller.
 */
